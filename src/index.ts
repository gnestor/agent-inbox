/**
 * Public type exports for @hammies/inbox.
 * Workspace plugins import these types:
 *
 *   import type { Plugin, PluginItem, PluginContext } from "@hammies/inbox"
 */

export type {
  Plugin,
  PluginItem,
  PluginContext,
  QueryResult,
  FieldDef,
  FieldType,
  BadgeConfig,
  FilterConfig,
  PluginComponents,
} from "./types/plugin.js"
